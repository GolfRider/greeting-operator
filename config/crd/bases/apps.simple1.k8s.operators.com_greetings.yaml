---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: greetings.apps.simple1.k8s.operators.com
spec:
  group: apps.simple1.k8s.operators.com
  names:
    kind: Greeting
    listKind: GreetingList
    plural: greetings
    singular: greeting
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          Greeting
          Greeting is the Schema for the greetings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Greeting
            properties:
              greeting:
                default: Hello
                description: Greeting word (optional, defaults to "Hello")
                type: string
              name:
                description: Name to greet (required)
                type: string
            required:
            - name
            type: object
          status:
            description: status defines the observed state of Greeting
            properties:
              message:
                description: The composed message
                type: string
              ready:
                description: Whether reconciliation succeeded
                type: boolean
            required:
            - ready
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
